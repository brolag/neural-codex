---
description: Compute Advantage calculator
argument-hint: ACTION="calc|log|history|report" [args]
---

Compute and log CA using `plans/metrics/ca-sessions.json`.
Formula:
CA = (compute_scaling * autonomy_min) / (human_time_min + effort + cost_usd)
