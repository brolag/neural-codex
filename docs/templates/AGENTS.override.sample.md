# AGENTS.override.md (Sample)

Use this file for local or temporary overrides. It should be short and specific.

## Overrides
- Add temporary constraints or context for a specific effort.
- Remove once no longer needed.

## Example
- Focus only on `docs/` and `scripts/`.
- Avoid touching `tests/` unless explicitly requested.
